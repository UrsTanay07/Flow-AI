PORT=3000
APP_VERSION=development
FLOWAI_LOG_REQUESTS=false
TRUST_PROXY=false

# Optional read-only traffic provider. The endpoint must return the
# TrafficSnapshot JSON shape documented in docs/ARCHITECTURE.md.
TRAFFIC_PROVIDER_URL=
TRAFFIC_PROVIDER_TOKEN=
